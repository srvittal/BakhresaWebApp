<div class="card-group">
    {{#each vehicle}}
    <div class="card">
        <div class="card-body">
            <p>{{name}}</p>
            <p>{{date}}</p>
            <p>{{time}}</p>
            <p>{{type}}</p>
            <p>{{reg}}</p>
        </div>
    </div>
    {{/each}}
</div>